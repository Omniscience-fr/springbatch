spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/springbatch?characterEncoding=utf8
  jpa:
    hibernate:
      ddl-auto: update
  batch:
    job:
      enabled: false
      names: parentnJob
server:
  port: 9090

